server:
#  image: ehazlett/interlock
#  image: bfirsh/interlock-haproxy-swarm:multihost
  image: alvinr/interlock-haproxy
  environment:
   - "DOCKER_HOST"
  volumes:
   - "/mnt/sda1/var/lib/boot2docker:/etc/docker"
  ports:
   - "80:8080"
backend:
  labels:
   - "com.interlock.networkmode=internal"
   - "com.interlock.port=5000"
  hostname: demo.dockercon.com

